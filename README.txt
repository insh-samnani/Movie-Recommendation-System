--Download the data set from this websit
(https://www.imdb.com/interfaces/)
--Run the code on Visual Studio Code 2019
(Create a new project -> Console Application C++)
--Add .tsv files with the same name as the data set files i.e "data111.tsv" and "Text.tsv"
--Now replace the imdb downloaded files with the .tsv files created